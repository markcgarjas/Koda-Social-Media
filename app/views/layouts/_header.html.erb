<nav class="navbar navbar-expand-lg navbar-dark bg-dark text-center text-white sticky-top border-bottom">
  <div class="container">
    <span class="navbar-brand"><%= link_to 'KSM', posts_path, class: "btn btn-outline-light btn-floating m-1" %></span>
    <div class="d-flex">
      <div class="btn-group" role="group">
        <% if user_signed_in? %>
          <%= link_to "Friends", friends_path, class: "btn btn-outline-light btn-floating m-1" %>
          <%= link_to "Find Friends", friend_requests_path, class: "btn btn-outline-light btn-floating m-1" %>
          <%= link_to pundit_user.email, user_post_lists_path(post: 'public'), class: "btn btn-outline-light btn-floating m-1" %>
        <% end %>
        <div class="btn-group">
          <button type="button" class="btn btn-outline-light btn-floating m-1 bi-list-task" data-bs-toggle="dropdown" aria-expanded="false"></button>
          <ul class="dropdown-menu dropdown-menu-end bg-light">
            <% if user_signed_in? %>
              <%= button_to "Log Out", destroy_user_session_path, { onclick: "return confirm('Are you sure you want to delete?')", method: :delete, class: "dropdown-item" } %>
            <% else %>
              <%= link_to "Login", new_user_session_path, class: "dropdown-item" %>
            <% end %>
          </ul>
        </div>
      </div>
    </div>
  </div>
</nav>